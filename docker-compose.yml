version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
      - "5000:5000"
    volumes:
      - ./server/data:/app/server/data
    environment:
      - PORT=5000
      - ADMIN_LOGINS=alix,admin,bde_admin,president_bde
